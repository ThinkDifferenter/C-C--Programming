#include<stdio.h>
#include<math.h>
int main()
{
	double y=1.0;
	int n,m;
	printf("请给定项数(>=2):");
	scanf("%d",&m);
	
	for(n=2;n<=m;n++)
	{
		y+=1.0/(n*(n-1));
	}
	printf("1+1/(1*2)+....+1/n(n-1)=%lf\n",y);
	return 0;
} 